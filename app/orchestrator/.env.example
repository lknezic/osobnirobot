# Orchestrator config
ORCHESTRATOR_PORT=3500
ORCHESTRATOR_SECRET=your-secret-here

# Docker
DOCKER_SOCKET=/var/run/docker.sock
CONTAINER_IMAGE=instantworker/worker:latest

# AI API Keys — direct mode (used if LITELLM_URL is not set)
GOOGLE_AI_KEY=your-google-ai-key
ANTHROPIC_API_KEY=your-anthropic-key

# LiteLLM proxy — recommended for production (containers never see real API keys)
# When set, GOOGLE_AI_KEY and ANTHROPIC_API_KEY above are ignored for containers.
# Real keys go in /opt/instantworker/infra/litellm.env instead.
# LITELLM_URL=http://172.17.0.1:4000
# LITELLM_KEY=sk-iw-litellm-your-master-key

# Worker templates path (absolute path on host)
WORKER_TEMPLATES_PATH=/opt/instantworker/worker-templates

# Port ranges for containers (expanded for multi-employee)
GATEWAY_PORT_START=20000
GATEWAY_PORT_END=21999
NOVNC_PORT_START=22000
NOVNC_PORT_END=23999
